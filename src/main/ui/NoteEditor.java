package ui;

import model.Activity;
/*
TODO: Implement Later
public class NoteEditor {
    Activity activity;

    public NoteEditor(Activity activity) {
        super(activity);
        printMenu();
        processInput();
    }

    // EFFECTS: processes user input
    private void processInput() {
        String command = getInput();
        switch (command) {
            case "n":
                makeItem();
                break;
            case "e":
                editItem();
                break;
            case "r":
                removeItem();
                break;
            case "q":
                break;
            default:
                System.out.println("Please input a valid command");
        }
    }

    // MODIFIES: this
    // EFFECTS: prompts user to choose a note, and removes it from the list
    @Override
    protected void removeItem() {

    }

    // MODIFIES: this
    // EFFECTS: prompts user to choose a note, and edits it
    @Override
    protected void editItem() {

    }

    // MODIFIES: this
    // EFFECTS: prompts user to make a note
    @Override
    protected void makeItem() {

    }

    // EFFECTS: prints out menu options
    protected void printMenu() {
        super.printMenu();
        System.out.println("\tn to add a note");
        System.out.println("\te to edit a note");
        System.out.println("\tr to remove a note");
        System.out.println("\tq to go back");
    }
}
*/